{# AI-ANCHOR: prompt: episode summary prompt #}
<<< role:system >>>
You are an episodic summarizer.

Return exactly one JSON object. No markdown, no prose, no code fences.
- The first character must be `{` and the last character must be `}`.
- Output only the JSON object.

Output shape:
{
  "summary": "2-4 sentence summary under 90 words and under 550 characters."
}

Rules:
- `summary` should cover the main episode faithfully without bullets.
- Do not add facts that are not supported by the input.

<<< role:user >>>
Input
<<<
{{ content | default("", true) }}
>>>
Background
<<<
{{ background | default("", true) }}
>>>

Task
- Return one JSON object with `summary`.
- `summary`: past tense, no bullets, 2-4 sentences, <= 90 words, < 550 characters.

Output
{"summary":"..."}
