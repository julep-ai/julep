[tasks]
format = "ruff format"
lint = "ruff check"
typecheck = "ty check src/julep_cli"
check = [
    "lint",
    "format",
    "typecheck",
]

[tasks.test]
env = { JULEP_CLI_TESTING = "true", PYTHONPATH = "{PYTHONPATH}:." }
cmd = "ward test --exclude .venv"
