{
  "name": "composable-serverless-agents-merged",
  "private": true,
  "version": "0.2.0",
  "description": "Composable serverless agents: pure core IR, frozen MCP/native tools, Temporal runtime, pomset projection.",
  "type": "module",
  "packageManager": "npm@10",
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && node --test test/*.test.mjs",
    "clean": "rm -rf packages/*/dist examples/*/dist *.tsbuildinfo packages/*/*.tsbuildinfo examples/*/*.tsbuildinfo"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
