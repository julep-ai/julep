# Pointer only: the canonical generic runtime image Dockerfile lives at
# tooling/runtime-image/Dockerfile.
#
# tooling/k3d-cad-demo/up.sh builds from that canonical Dockerfile so this demo
# cannot drift into a second runtime image definition.
