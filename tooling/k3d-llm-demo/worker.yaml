apiVersion: apps/v1
kind: Deployment
metadata:
  name: ca-llm-worker
spec:
  replicas: 1
  selector:
    matchLabels: {app: ca-llm-worker}
  template:
    metadata:
      labels: {app: ca-llm-worker}
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: worker
          image: ca-worker:llm-demo
          imagePullPolicy: Never   # imported via `k3d image import`
          env:
            - name: TEMPORAL_ADDRESS
              value: host.k3d.internal:7233
            - name: TEMPORAL_TASK_QUEUE
              value: composable-agents
            - name: WORKER_GRACEFUL_SHUTDOWN_S
              value: "20"
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef: {name: llm-keys, key: ANTHROPIC_API_KEY}
          ports:
            - containerPort: 8080
              name: health
          livenessProbe:
            httpGet: {path: /healthz, port: health}
            periodSeconds: 5
          readinessProbe:
            httpGet: {path: /readyz, port: health}
            periodSeconds: 2
